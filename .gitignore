build*/
_deps/
*.o
*.a
*.tar.xz

# Local IDE and generated files
.cache/
.clangd/index/
compile_commands.json
/.clangd
/.clang-format

# Local-only design notes and validation material
/demos/
/无线HDMI投屏系统应用层软件设计方案.md

# Deployment and validation scripts are kept outside the public source release.
scripts/
**/*.sh

# Extracted demo dependencies; use the archive locally when needed.
demos/ble/bluez-5.66/

# Do not publish nested repository metadata from vendored sources.
**/.git/

# Local logs and deployment overrides
*.local
*.log
